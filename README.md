# Resume

My resume, built with Typst. The core files are in the src directory and can be easily modified to fit your needs. The main.pdf file is the compiled version of the resume, which you can generate by running the Typst compiler on the source files.